---
deployment:
  tasks:
    - export DEPLOYPATH=/home/username/kevincab
    - /bin/cp -R * $DEPLOYPATH
    - /bin/cp .env.production $DEPLOYPATH/.env
    - cd $DEPLOYPATH
    - npm install --production
    - npx prisma generate
    - npx prisma migrate deploy
    - npm run build
